package pt.up.fe.comp.cp2.core.ollir.optimizations.jmm;

RegAllocIf2 extends Object  {




.construct "<init>"().V {
    invokespecial(this, "<init>" "java.lang.Object").V;
    
}

.method public method(initB.i32, initC.i32, initE.i32, initF.i32).i32 {
   b.i32 :=.i32 initB.i32;

   c.i32 :=.i32 initC.i32;

   a.i32 :=.i32 b.i32 +.i32 c.i32;

   d.i32 :=.i32 initE.i32 +.i32 initF.i32;

   d.i32 :=.i32 d.i32 +.i32 initE.i32;

   tmp3.bool :=.bool a.i32 ==.bool 0.i32;
if (tmp3.bool) goto then0;
b.i32 :=.i32 a.i32 +.i32 d.i32;

goto endif0;
then0:
b.i32 :=.i32 a.i32 -.i32 d.i32;

endif0:

   i.i32 :=.i32 b.i32;

   ret.i32 i.i32;
}

}
