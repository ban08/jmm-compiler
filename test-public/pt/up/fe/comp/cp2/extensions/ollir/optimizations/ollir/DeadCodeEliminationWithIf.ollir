package pt.up.fe.comp.cp2.extensions.ollir.optimizations.jmm;

DeadCodeEliminationWithIf extends Object  {


    

    .construct "<init>"().V {
        invokespecial(this."java.lang.Object", "<init>").V;
    }

    .method public foo(x.i32).i32 {
        res.i32 :=.i32 x.i32;

        tmp0.bool :=.bool res.i32 >.bool 0.i32;
        if (tmp0.bool) goto then0;

            goto endif0;
    then0:
            res.i32 :=.i32 res.i32 -.i32 1.i32;

    endif0:

        ret.i32 0.i32;
    }

}
