package pt.up.fe.comp.cp2.extensions.ollir.optimizations.jmm;

NoOptimization extends Object  {


    

    .construct "<init>"().V {
        invokespecial(this."java.lang.Object", "<init>").V;
    }

    .method public foo(a.i32, b.i32).i32 {
        tmp0.bool :=.bool a.i32 <.bool b.i32;
        if (tmp0.bool) goto then0;
            i.i32 :=.i32 b.i32 +.i32 1.i32;

            goto endif0;
    then0:
            i.i32 :=.i32 a.i32 +.i32 1.i32;

    endif0:

        res.i32 :=.i32 i.i32 *.i32 a.i32;

        ret.i32 res.i32;
    }

}
