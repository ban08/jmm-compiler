package pt.up.fe.comp.cp1.extensions.semantics.arrays.semanticanalysis.multidim;

class MultiSizeIsIntExpressionFail {
    int createArray(int n1, boolean n2) {
        int[][][] a;
        a = new int[n1 + 2 * n1][true && n2][n1 / 4];
        return 1;
    }
}