package pt.up.fe.comp.cp3.core.jasmin.integration.ollir;
import util.MathUtils;
import util.ioPlus;

MonteCarloPi extends Object  {
    
    .construct "<init>"().V {
        invokespecial(this."java.lang.Object", "<init>").V;
    }

    .method public performSingleEstimate().bool {
        tmp0.i32 :=.i32 0.i32 -.i32 100.i32;
        temp_arg__0.i32 :=.i32 tmp0.i32;
        temp_arg__1.i32 :=.i32 100.i32;
        tmp1.i32 :=.i32 invokestatic(MathUtils, "random", temp_arg__0.i32, temp_arg__1.i32).i32;
        rand1.i32 :=.i32 tmp1.i32;

        tmp2.i32 :=.i32 0.i32 -.i32 100.i32;
        temp_arg__2.i32 :=.i32 tmp2.i32;
        temp_arg__3.i32 :=.i32 100.i32;
        tmp3.i32 :=.i32 invokestatic(MathUtils, "random", temp_arg__2.i32, temp_arg__3.i32).i32;
        rand2.i32 :=.i32 tmp3.i32;

        tmp4.i32 :=.i32 rand1.i32 *.i32 rand1.i32;
        tmp5.i32 :=.i32 rand2.i32 *.i32 rand2.i32;
        tmp6.i32 :=.i32 tmp4.i32 +.i32 tmp5.i32;
        tmp7.i32 :=.i32 tmp6.i32 /.i32 100.i32;
        squareDist.i32 :=.i32 tmp7.i32;

        tmp8.bool :=.bool squareDist.i32 <.bool 100.i32;
        if (tmp8.bool) goto then0;
            in_circle.bool :=.bool 0.bool;

            goto endif0;
    then0:
            in_circle.bool :=.bool 1.bool;

    endif0:

        ret.bool in_circle.bool;
    }

    .method public estimatePi100(n.i32).i32 {
        samples_so_far.i32 :=.i32 0.i32;

        samples_in_circle.i32 :=.i32 0.i32;

    while0:
            tmp9.bool :=.bool samples_so_far.i32 <.bool n.i32;
            if (!.bool tmp9.bool) goto endwhile0;
            tmp10.bool :=.bool invokevirtual(this.MonteCarloPi, "performSingleEstimate").bool;
            if (tmp10.bool) goto then1;

                goto endif1;
        then1:
                samples_in_circle.i32 :=.i32 samples_in_circle.i32 +.i32 1.i32;

        endif1:
            samples_so_far.i32 :=.i32 samples_so_far.i32 +.i32 1.i32;

            goto while0;
    endwhile0:

        tmp13.i32 :=.i32 400.i32 *.i32 samples_in_circle.i32;
        tmp14.i32 :=.i32 tmp13.i32 /.i32 n.i32;
        pi_estimate.i32 :=.i32 tmp14.i32;

        ret.i32 pi_estimate.i32;
    }

    .method public static main(args.array.String).V {
                temp_arg__4.i32 :=.i32 655356.i32;
        invokestatic(MathUtils, "setSeed", temp_arg__4.i32).V;

        num_samples.i32 :=.i32 invokestatic(ioPlus, "requestNumber").i32;

        tmp16.MonteCarloPi :=.MonteCarloPi new(MonteCarloPi).MonteCarloPi;
        invokespecial(tmp16.MonteCarloPi, "<init>").V;
        temp_arg__5.i32 :=.i32 num_samples.i32;
        tmp17.i32 :=.i32 invokevirtual(tmp16.MonteCarloPi, "estimatePi100", temp_arg__5.i32).i32;
        pi_estimate_times_100.i32 :=.i32 tmp17.i32;

                temp_arg__6.i32 :=.i32 pi_estimate_times_100.i32;
        invokestatic(ioPlus, "printResult", temp_arg__6.i32).V;

        ret.V ;
    }

}
