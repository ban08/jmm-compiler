package pt.up.fe.comp.cp3.core.jasmin.integration.ollir;
import util.ioPlus;

FindMaximum extends Object  {
    .field public test_arr.array.i32;
    .construct "<init>"().V {
        invokespecial(this."java.lang.Object", "<init>").V;
    }

    .method public find_maximum(arr.array.i32).i32 {
        i.i32 :=.i32 1.i32;

        maximum.i32 :=.i32 arr.array.i32[0.i32].i32;

    while0:
            tmp0.i32 :=.i32 arraylength(arr.array.i32).i32;
            tmp1.bool :=.bool i.i32 <.bool tmp0.i32;
            if (!.bool tmp1.bool) goto endwhile0;
            value.i32 :=.i32 arr.array.i32[i.i32].i32;
            tmp2.bool :=.bool maximum.i32 <.bool value.i32;
            if (tmp2.bool) goto then0;

                goto endif0;
        then0:
                maximum.i32 :=.i32 value.i32;

        endif0:
            i.i32 :=.i32 i.i32 +.i32 1.i32;

            goto while0;
    endwhile0:

        ret.i32 maximum.i32;
    }

    .method public build_test_arr().i32 {

        tmp4.array.i32 :=.array.i32 new(array, 5.i32).array.i32;
        putfield(this, test_arr.array.i32, tmp4.array.i32).V;

        tmp5.array.i32 :=.array.i32 getfield(this, test_arr.array.i32).array.i32;
        tmp5.array.i32[0.i32].i32 :=.i32 14.i32;

        tmp6.array.i32 :=.array.i32 getfield(this, test_arr.array.i32).array.i32;
        tmp6.array.i32[1.i32].i32 :=.i32 28.i32;

        tmp7.array.i32 :=.array.i32 getfield(this, test_arr.array.i32).array.i32;
        tmp7.array.i32[2.i32].i32 :=.i32 0.i32;

        tmp8.array.i32 :=.array.i32 getfield(this, test_arr.array.i32).array.i32;
        tmp9.i32 :=.i32 0.i32 -.i32 5.i32;
        tmp8.array.i32[3.i32].i32 :=.i32 tmp9.i32;

        tmp10.array.i32 :=.array.i32 getfield(this, test_arr.array.i32).array.i32;
        tmp10.array.i32[4.i32].i32 :=.i32 12.i32;

        ret.i32 0.i32;
    }

    .method public get_array().array.i32 {
        tmp11.array.i32 :=.array.i32 getfield(this, test_arr.array.i32).array.i32;
        ret.array.i32 tmp11.array.i32;
    }

    .method public static main(args.array.String).V {
        tmp12.FindMaximum :=.FindMaximum new(FindMaximum).FindMaximum;
        invokespecial(tmp12.FindMaximum, "<init>").V;
        fm.FindMaximum :=.FindMaximum tmp12.FindMaximum;

                invokevirtual(fm.FindMaximum, "build_test_arr").i32;

        tmp13.array.i32 :=.array.i32 invokevirtual(fm.FindMaximum, "get_array").array.i32;
        temp_arg__0.array.i32 :=.array.i32 tmp13.array.i32;
        tmp14.i32 :=.i32 invokevirtual(fm.FindMaximum, "find_maximum", temp_arg__0.array.i32).i32;
        max.i32 :=.i32 tmp14.i32;

                temp_arg__1.i32 :=.i32 max.i32;
        invokestatic(ioPlus, "printResult", temp_arg__1.i32).V;

        ret.V ;
    }

}
