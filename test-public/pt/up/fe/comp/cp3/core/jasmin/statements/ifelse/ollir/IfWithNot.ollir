package pt.up.fe.comp.cp3.core.jasmin.statements.ifelse.ollir;

IfWithNot extends Object  {
    
    .construct "<init>"().V {
        invokespecial(this."java.lang.Object", "<init>").V;
    }

    .method public method(a.bool).i32 {
        tmp0.bool :=.bool !.bool a.bool;
        if (tmp0.bool) goto then0;
            res.i32 :=.i32 0.i32;

            goto endif0;
    then0:
            res.i32 :=.i32 1.i32;

    endif0:

        ret.i32 res.i32;
    }

}
