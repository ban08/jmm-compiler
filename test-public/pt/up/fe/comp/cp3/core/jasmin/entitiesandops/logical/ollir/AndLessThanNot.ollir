package pt.up.fe.comp.cp3.core.jasmin.entitiesandops.logical.ollir;

AndLessThanNot extends Object  {
    
    .construct "<init>"().V {
        invokespecial(this."java.lang.Object", "<init>").V;
    }

    .method public method(a.bool, b.i32, c.i32, d.bool, e.bool).bool {

        if (a.bool) goto then0;
            andTmp0.bool :=.bool 0.bool;
            goto endif0;
    then0:
            tmp0.bool :=.bool b.i32 <.bool c.i32;

            andTmp0.bool :=.bool tmp0.bool;
    endif0:

        if (andTmp0.bool) goto then1;
            andTmp1.bool :=.bool 0.bool;
            goto endif1;
    then1:
            tmp1.bool :=.bool !.bool d.bool;

            if (tmp1.bool) goto then2;
                andTmp2.bool :=.bool 0.bool;
                goto endif2;
        then2:

                andTmp2.bool :=.bool e.bool;
        endif2:

            andTmp1.bool :=.bool andTmp2.bool;
    endif1:
        ret.bool andTmp1.bool;
    }

}
