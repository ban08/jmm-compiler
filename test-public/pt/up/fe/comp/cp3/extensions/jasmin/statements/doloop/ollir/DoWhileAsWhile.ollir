package pt.up.fe.comp.cp3.extensions.jasmin.statements.doloop.ollir;

DoWhileAsWhile extends Object  {
    
    .construct "<init>"().V {
        invokespecial(this."java.lang.Object", "<init>").V;
    }

    .method public method(limit.i32).i32 {
        i.i32 :=.i32 0.i32;

        tmp0.bool :=.bool i.i32 <.bool limit.i32;
        if (tmp0.bool) goto then0;

            goto endif0;
    then0:
        while0:
                i.i32 :=.i32 i.i32 +.i32 1.i32;
                tmp2.bool :=.bool i.i32 <.bool limit.i32;
                if (tmp2.bool) goto while0;

        endif0:

            ret.i32 i.i32;
        }

}
