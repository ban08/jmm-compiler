package pt.up.fe.comp.cp3.extensions.jasmin.statements.forloop.ollir;

ForSimple extends Object  {
    
    .construct "<init>"().V {
        invokespecial(this."java.lang.Object", "<init>").V;
    }

    .method public method(limit.i32).i32 {
        i.i32 :=.i32 0.i32;

    while0:
            tmp0.bool :=.bool i.i32 <.bool limit.i32;
            if (!.bool tmp0.bool) goto endwhile0;
            i.i32 :=.i32 i.i32 +.i32 3.i32;


            goto while0;
        endwhile0:

            ret.i32 i.i32;
        }

}
