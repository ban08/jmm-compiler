package core.semantics.extensions;

class LoopConditionsOk {
    int compute() {
        int i;
        i = 0;

        do {
            i = i + 1;
        } while (i < 2);

        for (i = 0; i < 2; i = i + 1) {
        }

        return i;
    }
}
